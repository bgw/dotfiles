# Define suffix aliases for files (default programs)
alias -s tex="$EDITOR"
alias -s md="$EDITOR"
alias -s markdown="$EDITOR"
alias -s rst="$EDITOR"
alias -s css="$EDITOR"
alias -s scss="$EDITOR"
alias -s sass="$EDITOR"
alias -s html=firefox
alias -s pdf=evince
