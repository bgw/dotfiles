return {
  {
    "folke/noice.nvim",
    opts = {
      cmdline = {
        -- don't use the default popup
        view = "cmdline",
      },
      presets = {
        command_palette = false,
      },
    },
  },
}
